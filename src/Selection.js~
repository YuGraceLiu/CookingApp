import PropTypes from "prop-types"

const Selection = ({options, onSelect}) => {
  return (
    <select multiple size="5" onChange={onSelect}>
      {options.map((ingredient) => {
            return <option value={ingredient}>
              {ingredient}
            </option>
          }
      )}
    </select>
  );
}


export const default Selection
